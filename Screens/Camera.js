import React, {useState} from 'react';
import { Text, View } from 'react-native';

export default () => {
    return(
        <View>
            <Text>This is the Camera page</Text>
        </View>
    );
}